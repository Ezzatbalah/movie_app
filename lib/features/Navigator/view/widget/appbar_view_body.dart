import 'package:flutter/widgets.dart';

class AppbarViewBody extends StatelessWidget {
  const AppbarViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
